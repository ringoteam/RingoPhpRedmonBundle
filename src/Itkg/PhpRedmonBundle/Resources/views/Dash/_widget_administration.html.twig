<div class="accordion-group well">
    <div class="accordion-heading">
      <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion2" href="#collapseOne">
        Administration
        <b class="caret pull-right"></b>
      </a>
    </div>
    <div id="collapseOne" class="accordion-body collapse">
      <div class="accordion-inner">
        <ul>
            {% for database in instance.getDatabases %}
                <li>
                    Base {{ database.name }}<a href="{{ path('itkg_php_redmon_admin_flushdb', {'id': database.id}) }}">
                    <button class="btn">Flush DB</button></a>
                </li>
            {% endfor %}
        </ul>
        <a href="{{ path('itkg_php_redmon_admin_flushall') }}">Supprimer toutes les données</a>
      </div>
    </div>
  </div>